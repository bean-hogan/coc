// ============================================================
// TORN GROUPS — Site Config
// ============================================================

const CONFIG = {
  // Apps Script Web App URL (fallback if data.json unavailable)
  SCRIPT_URL: 'https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec',

  // GitHub raw data.json — primary data source (fast, CDN-served)
  // Format: https://raw.githubusercontent.com/OWNER/REPO/main/data.json
  DATA_URL: 'https://raw.githubusercontent.com/bean-hogan/coc/main/data.json',
};
